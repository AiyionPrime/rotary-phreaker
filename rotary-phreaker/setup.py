#!/usr/bin/env python3
from distutils.core import setup

setup(name='rotary-phreaker',
      version='0.1',
      py_modules=['rotary-phreaker'],
      install_requires=[
          'RPi.GPIO'
      ]
      )
